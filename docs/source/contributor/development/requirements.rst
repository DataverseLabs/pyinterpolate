Requirements and dependencies (version >= 1)
============================================

Core requirements and dependencies are listed in ``pyproject.toml`` file.
Developers should install additional packages from the ``dev`` group.